{
  "name": "frontend",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "echo \"frontend dev script will be added when Angular is initialized\"",
    "build": "echo \"frontend build script will be added when Angular is initialized\"",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "echo \"frontend tests will be added when Angular is initialized\"",
    "lint": "echo \"frontend lint script will be added when Angular is initialized\""
  },
  "dependencies": {
    "@decision-lab/shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
