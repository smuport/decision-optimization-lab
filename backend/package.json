{
  "name": "backend",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "echo \"backend dev script will be added when NestJS is initialized\"",
    "build": "echo \"backend build script will be added when NestJS is initialized\"",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "echo \"backend tests will be added when NestJS is initialized\"",
    "lint": "echo \"backend lint script will be added when NestJS is initialized\""
  },
  "dependencies": {
    "@decision-lab/shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
